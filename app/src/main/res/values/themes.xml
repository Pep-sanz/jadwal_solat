<resources>
    <!-- Default App Theme -->
    <style name="Theme.JadwalShalat" parent="@style/Theme.Leanback" />

    <!-- Transparent Theme -->
    <style name="AppTheme.NoActionBar.Transparent" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
    </style>

    <!-- Cyan Theme -->
    <style name="AppTheme.Cyan" parent="Theme.AppCompat.NoActionBar">
        <item name="colorPrimary">@color/cyan_primary</item>
        <item name="colorPrimaryDark">@color/cyan_secondary</item>
        <item name="android:colorBackground">@color/cyan_background</item>
    </style>

    <!-- Red Theme -->
    <style name="AppTheme.Red" parent="Theme.AppCompat.NoActionBar">
        <item name="colorPrimary">@color/red_primary</item>
        <item name="colorPrimaryDark">@color/red_secondary</item>
        <item name="android:colorBackground">@color/red_background</item>
    </style>

    <!-- Green Theme -->
    <style name="AppTheme.Green" parent="Theme.AppCompat.NoActionBar">
        <item name="colorPrimary">@color/green_primary</item>
        <item name="colorPrimaryDark">@color/green_secondary</item>
        <item name="android:colorBackground">@color/green_background</item>
    </style>

    <!-- Yellow Theme -->
    <style name="AppTheme.Yellow" parent="Theme.AppCompat.NoActionBar">
        <item name="colorPrimary">@color/yellow_primary</item>
        <item name="colorPrimaryDark">@color/yellow_secondary</item>
        <item name="android:colorBackground">@color/yellow_background</item>
    </style>

    <!-- Blue Theme -->
    <style name="AppTheme.Blue" parent="Theme.AppCompat.NoActionBar">
        <item name="colorPrimary">@color/blue_primary</item>
        <item name="colorPrimaryDark">@color/blue_secondary</item>
        <item name="android:colorBackground">@color/blue_background</item>
    </style>
</resources>
